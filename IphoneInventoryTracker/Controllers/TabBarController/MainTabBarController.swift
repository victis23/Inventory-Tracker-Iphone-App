//
//  MainTabBarController.swift
//  IphoneInventoryTracker
//
//  Created by Scott Leonard on 10/31/19.
//  Copyright © 2019 Scott Leonard. All rights reserved.
//

// This controller is not conntected to anything!

/*
import UIKit

class MainTabBarController: UITabBarController {
	
	let list = InventoryTracker_CollectionViewController()
	let vendors = VenderList_TableViewController()
	
	
	override func viewDidLoad() {
		super.viewDidLoad()
		viewControllers = [setupTabBarControllers("List", UIImage(systemName: "house")!, list), setupTabBarControllers("venders", UIImage(systemName: "info.circle")!, vendors)]
	}
	
	func setupTabBarControllers(_ title: String,
								_ image : UIImage,
								_ viewController : UIViewController)-> UINavigationController{
		
		let vc = UINavigationController(rootViewController: viewController)
		vc.tabBarItem.title = title
		vc.tabBarItem.image = image
		return vc
	}
	
	
	/*
	// MARK: - Navigation
	
	// In a storyboard-based application, you will often want to do a little preparation before navigation
	override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
	// Get the new view controller using segue.destination.
	// Pass the selected object to the new view controller.
	}
	*/
	
}

*/
